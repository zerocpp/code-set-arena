services:
  codesetarena-teacher:
    image: codesetarena-teacher:v7.1.9
    ports:
      - "${CODESETARENA_TEACHER_PORT:-8010}:8010"
    volumes:
      - ./data:/data
    environment:
      CODESETARENA_TEACHER_DATA_DIR: /data
